# ML Artifacts

The backend loads these artifacts lazily:

```text
kmeans_model.joblib
scaler.joblib
spend_predictor.cbm
ctr_model.cbm
metadata.json
```

They come from `322kirpich/ml_moscow_transport` at commit `4779dba`.
Runtime feature contracts are documented in the root `README.md`.
